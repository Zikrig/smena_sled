# Смена След - Telegram Bot для охраны

Telegram-бот для управления сменами охраны с функциями обхода, осмотра транспорта и сообщения о проблемах.

## Возможности

- 📸 Начало смены с фотофиксацией
- 📋 Передача ТМЦ
- 🚶 Обход объекта (неограниченное количество фото)
- 🔍 Осмотр транспорта (неограниченное количество фото)
- ⚠️ Сообщения о проблемах
- 🚨 Экстренные вызовы

## Быстрый старт с Docker

### 1. Клонирование и настройка

```bash
git clone <repository-url>
cd smena_sled
```

### 2. Настройка переменных окружения

```bash
cp env.example .env
```

Отредактируйте файл `.env` и укажите:
- `BOT_TOKEN` - токен вашего Telegram бота
- `GROUP_ID` - ID группы для отправки сообщений

### 3. Запуск с Docker Compose

```bash
# Сборка и запуск
docker-compose up --build

# Запуск в фоновом режиме
docker-compose up -d --build

# Просмотр логов
docker-compose logs -f

# Остановка
docker-compose down
```

### 4. Альтернативный запуск с Docker

```bash
# Сборка образа
docker build -t smena-sled-bot .

# Запуск контейнера
docker run -d \
  --name smena-sled-bot \
  --env-file .env \
  --restart unless-stopped \
  smena-sled-bot
```

## Разработка

### Локальная установка

```bash
# Установка зависимостей
pip install -r requirements.txt

# Запуск
python main.py
```

### Структура проекта

```
smena_sled/
├── handlers/          # Обработчики команд
│   ├── start.py       # Начало смены
│   ├── shift.py       # Управление сменами
│   ├── patrol.py      # Обход объекта
│   ├── inspection.py  # Осмотр транспорта
│   ├── problem.py     # Сообщения о проблемах
│   └── emergency.py   # Экстренные вызовы
├── keyboards.py       # Клавиатуры бота
├── states.py          # Состояния FSM
├── config.py          # Конфигурация
├── main.py           # Точка входа
└── requirements.txt  # Зависимости
```

## Команды бота

- `/start` - Главное меню
- `📸 Начать смену` - Начало новой смены
- `📋 Передача ТМЦ` - Передача товарно-материальных ценностей
- `🚶 Обход` - Обход объекта охраны
- `🔍 Осмотр` - Осмотр транспорта
- `⚠️ Проблема` - Сообщение о проблеме
- `🚨 Экстренный вызов` - Экстренный вызов служб

## Переменные окружения

| Переменная | Описание | Пример |
|------------|----------|---------|
| `BOT_TOKEN` | Токен Telegram бота | `1234567890:ABCdef...` |
| `GROUP_ID` | ID группы для уведомлений | `-1001234567890` |

## Логирование

Логи сохраняются в директории `logs/` и доступны через Docker volumes.

## Безопасность

- Контейнер запускается от непривилегированного пользователя
- Переменные окружения загружаются из `.env` файла
- Чувствительные данные не попадают в образ Docker
